default_stages: [manual]

repos:
  - repo: local
    hooks:
      - id: flake8
        name: flake8
        entry: flake8 project/
        language: system
        types: [python]
        require_serial: true
        verbose: true
      - id: black
        name: black
        entry: black --check --diff project/
        language: system
        types: [python]
        require_serial: true
      - id: isort
        name: isort
        entry: isort --diff --check project/
        language: system
        types: [python]
        require_serial: true
        verbose: true
      - id: pyright
        name: pyright
        entry: pyright project/
        language: system
        types: [python]
        require_serial: true
        verbose: true
